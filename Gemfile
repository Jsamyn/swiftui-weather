source "https://rubygems.org"

# This file describes which dependencies are needed to run a ruby program
# In this project, we only need Fastlane at the moment and that is what we list
# as a dependency.
gem "fastlane"
gem "dotenv"
